<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.plainpub.videomanager.repository.IActorRepository">

    <select id="getAllActor" resultType="Actor">
        SELECT id, name, alias, created_time as createdTime, updated_time as updatedTime
        FROM actor
    </select>

    <select id="getActor" resultType="Actor">
        SELECT id, name, alias, created_time as createdTime, updated_time as updatedTime
        FROM actor
        WHERE id =#{id}
    </select>


</mapper>